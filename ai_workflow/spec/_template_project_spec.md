# 项目级 Spec（规约）

> 复制本文件到项目（如 `docs/spec_project.md`），填写后供 AI 与团队共用。新对话时先让 AI 读此 Spec 再写代码。

---

## 1. 项目概述

- **项目名称**：
- **一句话定位**：
- **目标用户/场景**：

---

## 2. 技术栈与版本

| 类别 | 选型 | 版本/备注 |
|------|------|-----------|
| 语言 |  |  |
| UI 框架 |  |  |
| 网络/存储 |  |  |
| 构建/包管理 |  |  |
| 其他关键依赖 |  |  |

---

## 3. 目录与模块结构

```
（贴项目根目录结构，或说明核心模块职责）
例如：
/app or /src
  /ui         - 界面
  /data       - 数据层
  /domain     - 业务逻辑
  /server     - 若有多端，服务端入口
```

- **多端说明**（若有）：Android / iOS / Desktop / Web 各自入口与共享代码位置。
- **命名约定**：包名、文件命名、分支命名等。

---

## 4. 数据与接口约定

- **数据源**：本地 DB / 远程 API / 混合。
- **模型/实体**：放在哪一层、谁定义（如 `shared`、`data/models`）。
- **API 风格**：REST/GraphQL、基础 URL、鉴权方式（若适用）。

---

## 5. UI / 交互约定

- **设计规范**：Material / 自定义 / 设计稿来源。
- **主题与字体**：亮暗色、字号层级。
- **导航**：底部 Tab / 侧栏 / 单页 等。

---

## 6. 质量与工程

- **测试**：单测/UI 测范围，放哪。
- **静态检查**：Lint/Formatter 规则。
- **提交流程**：分支策略、Code Review 要求（若适用）。

---

## 7. 本项目的「不要」

- 明确不用的库、不采用的方案、禁止的写法（减少 AI 乱用）。

---

## 8. 变更记录（可选）

| 日期 | 变更内容 |
|------|----------|
|  |  |
