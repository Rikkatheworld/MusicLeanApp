# WORKFLOW_INIT：<工作流名称> 的入参收集

> 当该 Workflow 需要「多种、复杂」入参时使用。先执行本 INIT，让 AI 生成一份结构化问卷/文档，用户填完后作为 WORKFLOW 的输入。

---

## 何时使用

- 需要同时提供：需求文档、设计稿、接口文档、环境信息等。
- 希望「通用 Workflow」与「具体需求」解耦，通过填写一份表再执行。

---

## 本 INIT 要收集的信息

（列出 AI 应生成的问卷项，或模板字段）

| 项 | 说明 | 示例/格式 |
|----|------|-----------|
| 需求摘要 |  |  |
| 设计稿/链接 |  |  |
| 接口文档路径 |  |  |
| 其他 |  |  |

---

## 输出格式

- AI 执行本 INIT 后，应产出一份可编辑的文档（如 Markdown 表），用户填写后保存。
- 执行主 WORKFLOW 时，以该文档为入参。

---

## 执行方式

- 对话中先执行：「按 WORKFLOW_INIT.md 生成入参表，我填好后发你，再按 WORKFLOW.md 执行。」
